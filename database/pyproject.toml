[project]
name = "wikart-database"
version = "0.1.0"
description = "WIKART - couche base de données Neo4j (schéma, import, requêtes métier, GDS, admin)"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "neo4j>=6.2.0",
    "pandas>=3.0.5",
    "python-dotenv>=1.2.2",
]

[tool.uv]
package = false
